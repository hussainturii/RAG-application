# RAG-application
Local document vault with RAG-based Q&amp;A using Ollama LLMs. Upload PDFs, TXT, DOCX, or Excel files and ask natural language questions. Returns verbatim citations with source references before optional AI-generated answers. Features persistent vector storage with ChromaDB, cross-encoder reranking, and relevance filtering to prevent hallucinations.
